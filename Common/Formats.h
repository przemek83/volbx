#ifndef FORMATS_H
#define FORMATS_H

/**
 * @brief data formats.
 */
enum DataFormat
{
    DATA_FORMAT_UNKNOWN = -1,
    DATA_FORMAT_STRING,
    DATA_FORMAT_FLOAT,
    DATA_FORMAT_DATE
};

#endif // FORMATS_H
